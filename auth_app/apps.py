"""Django app configuration for auth_app."""

from django.apps import AppConfig


class AuthAppConfig(AppConfig):
    """Application configuration for auth domain models and APIs."""

    name = "auth_app"
    verbose_name = "ACTIVE TOKENS"
