"""Django app configuration for content_app."""

from django.apps import AppConfig


class ContentAppConfig(AppConfig):
    """Application configuration for content features."""

    name = 'content_app'
